micronaut:
  application:
    name: carros-grpc-test
datasources:
  default:
    url: jdbc:h2:mem:testDb

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true

